# Sample subjects

Finished artifacts for trying the skill. Each folder has a subject
file. `expected.md`, where present, names the injected faults. The
skill writes the critique; nothing here is a command-line corpus.

A finding on `clean/` that is only a wording preference is a miss
of the product.
